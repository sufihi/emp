import { StatusoptionDirective } from './statusoption.directive';

describe('StatusoptionDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusoptionDirective();
    expect(directive).toBeTruthy();
  });
});
